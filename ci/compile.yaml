platform: linux
image_resource:
  type: docker-image
  source:
    repository: golang
    tag: 1.6.2

inputs:
  - name: toolbox-repo

outputs:
  - name: toolbox-bin

run:
  path: toolbox-repo/ci/compile
  args:
    - toolbox-repo
    - toolbox-bin
